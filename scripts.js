// start the new grid

let gridSize = 5;
// for loop to create gridsize rows 

function makeRows(gridSize){
    let rowArray = [];
    for(let i = 0; i < gridSize; i++){

    }
}

// use loop to create gridsize amount of divs for rows and buttons for each row and 
// create event listeners for each button
// event listeners in for loop will change style and html in each button

// function to check winner
// function to check for tie
// function to generate computer turn